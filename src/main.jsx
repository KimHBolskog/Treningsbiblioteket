import React from "react";
import { createRoot } from "react-dom/client";
import BilingualSite from "./App.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BilingualSite />
  </React.StrictMode>
);
